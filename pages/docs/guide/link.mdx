# Next.js 链接

所有相对的 Markdown 链接都会自动转换为 Next.js 链接。这意味着目标页面将被预加载。当您点击一个链接时，页面将在客户端加载，就像一个 SPA（单页应用）一样，而不会进行完整的页面加载。例如：

```md
点击[这里](/about)阅读更多内容.
```

将等同于：

```jsx
import Link from 'next/link'

点击 <Link href="/about">这里</Link> 阅读更多内容.
```

此功能使在 Nextra 页面之间的导航快速且无缝。
